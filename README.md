# Amiriset Meta Manager
WordPress Plug-In. Per-page/post SEO meta tags (description, keywords, OG, custom) stored as a single JSON object. Supports pages, posts and all CPTs. Built-in keyword extractor.
